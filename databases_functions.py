def not_in_database(id: int) -> bool:
    return True  # переделать


def insert_full_name(id: int, full_name: str) -> bool: #возращает True если произошла ошибка
    return False

def insert_sex(id: int, sex: int) -> bool: #возращает True если произошла ошибка
    return False